//NOTE: Up to 99 arguments

//SELECT --> CONCAT

#include "./Miscellaneous.h"
#include "./RemoveParenthesisInList.h"  //COMPOSE, COMPOSE2, SELECT, MULTI_CONCAT
#include "./AppendListsItems.h"         //COMPOSE3, COMPOSE4, SELECT2
#include "./ArgsCount.h"                //COMPOSE5, COMPOSE6, MULTI_CONCAT2, (Internal: COMPOSE7, COMPOSE8, MULTI_CONCAT3)
#include "./ConcatListsItems.h"         //COMPOSE9, SELECT3
#include "./GetLastArg.h"               //COMPOSE10, SELECT4
#include "./AreArgsEmpty.h"             //COMPOSE11, COMPOSE12, MULTI_CONCAT4, (Internal: COMPOSE13, COMPOSE14, MULTI_CONCAT5)
#include "./Overload.h"                 //COMPOSE15, COMPOSE16, SELECT6, (Internal: COMPOSE17, COMPOSE18, SELECT7)
#include "./PrefixSuffixArgs.h"         //COMPOSE19, COMPOSE20, COMPOSE21, CONCAT8, SELECT8
#include "./PrependAppendArgs.h"        //COMPOSE22, COMPOSE23, COMPOSE24, SELECT9, CONCAT9
#include "./CountTo.h"                  //COMPOSE25, COMPOSE26, SELECT10, COMPOSE27, COMPOSE28, SELECT11, COMPOSE29, COMPOSE30, SELECT12, COMPOSE31, COMPOSE32, SELECT13
#include "./PersistentCounter.h"
